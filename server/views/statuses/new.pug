extends ../layouts/application.pug

block header
  = t('views.statuses.new.title')

block content
  +formFor(status)(action=route('statuses') method='post')
    +input(status, 'statusName', errors)(type="text")
    +submit(status)(value=t('views.statuses.new.submit'))